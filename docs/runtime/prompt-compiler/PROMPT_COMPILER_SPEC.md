# Prompt Compiler Spec

The Prompt Compiler transforms:

```text
human request + brand pack + visual canon rules + output goal
```

into:

```text
production-grade prompt + negative constraints + review checklist
```

## Responsibilities

- extract output goal
- apply brand pack rules
- apply visual canon rules
- include target format
- define negative constraints
- define quality criteria
